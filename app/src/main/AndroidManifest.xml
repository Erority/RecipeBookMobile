<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipebook">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeBook">
        <activity
            android:name=".view.recipe_activity.RecipeActivity"
            android:exported="false" />
        <activity
            android:name=".view.add_new_recipe.AddNewRecipeActivity"
            android:exported="false" />
        <activity
            android:name=".view.base_tab_activity.BaseTabActivity"
            android:exported="false" />
        <activity
            android:name=".view.base_auth_activity.BaseAuthActivity"
            android:exported="false" />
        <activity
            android:name=".view.startscreen.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.MainActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>